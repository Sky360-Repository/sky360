# FIXME: Add semantic versioning for the repository and the docker images
#   for reference: 
#     https://github.com/marketplace/actions/github-tag-with-semantic-versioning
#     https://github.com/marketplace/actions/tag-version
# FIXME: add metadata to docker hub (description, readme.md)


name: Docker Image - Cyclop

on:
  push:
    branches: [ "main" ]
    paths:
      - Docker/**
      - .github/workflows/docker-image.yml
  pull_request:
    branches: [ "main" ]
    paths:
      - Docker/**
env:
  DOCKER_IMAGE: sky360/cyclop
jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and push
      uses: docker/build-push-action@v3
      with:
        context: "./docker/cyclop"
        platforms: linux/arm64,linux/amd64
        push: true
        tags: ${{ env.DOCKER_IMAGE }}:latest
    - name: Update Docker Hub Description and Readme
      uses: peter-evans/dockerhub-description@v3
      with:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: ${{ env.DOCKER_IMAGE }}
        short-description: ${{ github.event.repository.description }}
        readme-filepath: ./docker/cyclop/README.md
